spring.application.name=Profile
server.port=${PORT:8080}

# Avoid stale JS/CSS in the browser while iterating on the UI
spring.web.resources.cache.cachecontrol.no-store=true
spring.thymeleaf.cache=false


# Gmail SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:phantienthinh0907@gmail.com}
spring.mail.password=${MAIL_PASSWORD:nxie ahdt cche nrhy}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Email recipient
contact.email.to=${MAIL_TO:phantienthinh0909@gmail.com}
